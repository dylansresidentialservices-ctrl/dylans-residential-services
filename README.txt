FIX 4 - PWA metadata

This version explicitly links the Web App Manifest, theme metadata and icons in the HTML head,
and registers the service worker. Deploy all files as the production site.

After deployment, open the site once, then return to PWABuilder and re-enter the site URL.
PWABuilder should detect the manifest and service worker.
